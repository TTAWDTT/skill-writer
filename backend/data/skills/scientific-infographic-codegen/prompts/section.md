任务：为“{{ diagram_title }}”生成图示结构化 JSON（用于本地代码渲染，非成图模型）。

图类型：{{ diagram_type }}

请按以下顺序执行：

步骤1）概括（必须）
- 先给出 3-6 条 concise_summary（短语级要点）。
- 重点片段优先，全文上下文用于校准术语与逻辑。

步骤2）结构化（必须）
- 再输出 diagram_spec，严格匹配 schema。
- 技术路线建议体现：问题定义→方法实现→验证评估→成果输出。
- 研究框架建议体现：目标→问题/假设→WP→支撑→成果。

输出规则（刚性约束）：
1. 严格只输出 JSON；
2. 不得复制长段落（尤其禁止整句复写到多个阶段）；
3. 每个 bullets 项建议 8-20 字；
4. 术语口径保持一致，不得同义词混写；
5. 不得编造事实、数据、结果或结论；
6. 若信息不足，允许使用中性描述，但不能写“待补充”等占位词。

目标 JSON schema：
{{ schema_hint }}

输出 JSON 目标格式：
{
  "concise_summary": ["摘要要点1", "摘要要点2", "摘要要点3"],
  "diagram_spec": { ...严格按 schema 的对象... }
}

输入A（全文上下文，背景参考）：
{{ full_context }}

输入B（重点片段，核心优先）：
{{ focus_context }}
